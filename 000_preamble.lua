-- file <- "000_preamble.lua"
--
-----------------------------------------------------------------------
-- PURPOSE: cheat sheet, examples of commonly lua code 
-- USAGE:   If line begins with `lua`, it is to be yanked run at command line `:lua
-- ...`
--          Otherwise, it can be run :a,bsource where `a` and `b` refer to
--          `range`.

-----------------------------------------------------------------------
--

